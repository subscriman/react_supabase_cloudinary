# 수집 정확도 고도화 체크리스트 (배포 전 확정안)

기준일: 2026-04-04

## 1) 배포 게이트(공통)

- [x] 사이트별 수집 방식(어댑터) 확정
- [x] `audit-sites` 기준 리포트 자동 생성
- [x] `fetch failed` 원인 상세 로그(코드/원인) 출력
- [x] `dry-run` 기본값을 실데이터 검증 모드로 고정  
  - 샘플 fallback은 `INGESTION_DRYRUN_SAMPLE_FALLBACK=true`일 때만 사용
- [x] DB `source_sites.list_url`와 무관하게 코드의 최신 `site-config` URL을 우선 사용

## 2) 사이트별 확정 전략

| 사이트 키 | 확정 전략 | 검증 결과 |
|---|---|---|
| `mmca` | 목록 HTML + 상세 링크 | 통과 |
| `sac` | `schedule?tab=3` + `getProgramCalList` API (`CATEGORY_PRIMARY=6`) | 통과 |
| `warmemo` | `boardList` 전용 파서 + TLS 예외 처리 | 통과 |
| `sema` | whatson landing 카드 파서(현재/예정) | 부분 통과(일부 날짜 누락) |
| `museum` | `M020201(current)` + `M020202(upcoming)` 전용 파서 | 통과 |
| `busan-art` | 현재/예정 목록 페이지 파서 | 통과 |
| `leeum` | 공식 list API 파서 | 통과 |
| `apma` | `pages` JSON 파서 | 통과 |
| `kukje` | 전시 목록 카드 파서 | 통과 |
| `ddp` | `menuno=240` 전시 목록 파서 | 통과 |
| `sejong` | `performListData` AJAX 파서 | 통과 |
| `daegu-art` | 현재전시(`menu_id=00000729`) 카드 파서 + 필요 시 Edge fetch fallback | 통과(옵션 활성 기준) |

## 3) 검증 커맨드 체크리스트

- [x] `npm run ingest:audit-sites -- --sample=2`
- [x] `npm run ingest:audit-sites -- --site=sac,museum --sample=3`
- [x] `npm run ingest:run -- --site=sac,museum,warmemo --dry-run --limit=3`

## 4) 배포 전 최종 결정 필요 항목

- [x] `daegu-art` 처리 정책 확정: 기본 비활성 + 옵션 활성(`INGESTION_ENABLE_DAEGU_ART=true`)
- [x] 연결 불안정 환경 대응: `INGESTION_DAEGU_USE_EDGE_FETCH=true`로 Edge fallback 사용
- [x] `sema` 날짜 누락 케이스 보강 완료 (`상시` 전시 상세 페이지 날짜 보강 적용, valid 100%)

## 5) 승인 기준(운영 시작선)

- [x] 운영 대상 11개 사이트 + 옵션 대상(`daegu-art`) 수집 경로 확정
- [x] 정상 사이트의 `detailUrl`/`valid schema` 비율 100% 또는 근접
- [x] 장애 사이트는 원인 메시지와 재현 커맨드 확보

## 6) 대구미술관 운영 커맨드(옵션)

- [x] `npm run ingest:daegu:audit`
- [x] `npm run ingest:daegu:dry-run`
- [x] `npm run ingest:daegu:audit:edge`
- [x] `npm run ingest:daegu:dry-run:edge`
- [x] 진단 리포트 분리 저장: `docs/수집_사이트_진단_리포트_daegu.md`

## 7) 이미지 품질 필터 확정(더미/안내 이미지 제외)

- [x] 후보 이미지 수집 단계에서 더미 패턴 URL 제외
  - `/noimage/`
  - `/images/common/logo`
  - `/design/common/images/asset/noimage`
  - `/cmsh/*/images/common/*`
- [x] SAC 전용 더미 경로 제외
  - `/design/theme/sac/images/logo*`
  - `/design/theme/sac/images/sub/*`
- [x] 국립중앙박물관 공통 아이콘/QR 경로 제외
  - `/museum/api/qrcode.do*`
  - `/ux/content/images/common/*`
  - `/design/common/images/*`
  - `/design/content/museum/images/*`
- [x] 상세 페이지 파싱(`og:image`, inline img) 단계에서도 동일 필터 적용
- [x] 저장 fallback 정책 보강
  - 스토리지 업로드 실패 시에도 placeholder URL은 `poster_image_url`로 저장하지 않음
- [x] 실데이터 검증 완료
  - 2026-04-04 재수집 기준 `스페인 미술 500년(외부ID: 77679)`의 `poster_image_url=null` 확인
  - 2026-04-04 재수집 기준 `보존과학, 새로운 시작 함께하는 미래(외부ID: 3181575)`는 `crosseditor/binary/images/*`로 저장 확인

## 8) 전수 검증 이력

- [x] 예술의전당 외 전체 사이트 포함 전수 검증 완료
  - 상세 결과: `docs/수집_전수_검증_체크리스트_20260404.md`
