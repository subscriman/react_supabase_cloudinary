# 수집 전수 검증 체크리스트 (2026-04-04)

기준: 예술의전당 외 전체 수집 대상 포함, 사이트별 `진단(audit)` + `실수집(limit=1)` 검증

## 1) 실행 체크리스트

- [x] 11개 운영 사이트 전수 진단 실행
  - `npm run ingest:audit-sites -- --sample=2 --output=docs/수집_사이트_진단_리포트_20260404_전수.md`
- [x] 대구미술관(옵션) 진단 실행(Edge fallback)
  - `npm run ingest:daegu:audit:edge`
- [x] 11개 운영 사이트 실수집(limit=1) 실행
  - `npm run ingest:run -- --site=mmca,sac,warmemo,sema,museum,busan-art,leeum,apma,kukje,ddp,sejong --limit=1`
- [x] 대구미술관(옵션) 실수집(limit=1) 실행(Edge fallback)
  - `INGESTION_ENABLE_DAEGU_ART=true INGESTION_DAEGU_USE_EDGE_FETCH=true node scripts/ingestion/run.js --site=daegu-art --limit=1`

## 2) 사이트별 검증 결과

| 사이트 키 | 진단(audit) | 실수집 최신 상태 | 비고 |
|---|---|---|---|
| `mmca` | 통과(raw=8, valid=100%, detail=100%) | `accepted` | 이미지 `main=ok` |
| `sac` | 통과(raw=14, valid=100%, detail=100%) | `accepted` | 더미 이미지 필터 반영 상태 확인 |
| `warmemo` | 통과(raw=1, valid=100%, detail=100%) | `accepted` | 이미지 경고 1건(저장 자체는 정상) |
| `sema` | 통과(raw=12, valid=100%, detail=100%) | `accepted` | 이미지 `main=none` 케이스 정상 처리 |
| `museum` | 통과(raw=3, valid=100%, detail=100%) | `accepted` | 신규 insert 확인 |
| `busan-art` | 통과(raw=1, valid=100%, detail=100%) | `accepted` | 이미지 `main=ok` |
| `leeum` | 통과(raw=5, valid=100%, detail=100%) | `accepted` | `endDate` 누락 보강 로직 적용 후 재검증 통과 |
| `apma` | 통과(raw=2, valid=100%, detail=100%) | `accepted` | 이미지 `main=ok` |
| `kukje` | 통과(raw=1, valid=100%, detail=100%) | `accepted` | 이미지 `main=ok` |
| `ddp` | 통과(raw=3, valid=100%, detail=100%) | `accepted` | 이미지 `main=ok` |
| `sejong` | 통과(raw=4, valid=100%, detail=100%) | `accepted` | 이미지 경고 2건(저장 자체는 정상) |
| `daegu-art` | 통과(raw=4, valid=100%, detail=100%) | `accepted` | 기본 fetch 실패 시 Edge fallback 경로로 성공 |

## 3) 보강 반영 사항

- [x] LLM 정규화 결과가 일부 필드를 비워도 원문 fallback 값으로 자동 보강
  - 대상: `scripts/ingestion/run.js`, `web/scripts/ingestion/run.js`
  - 효과: `leeum`의 `endDate 누락` 반려 케이스 해소
- [x] 국립중앙박물관 공통 아이콘/QR 이미지 경로 필터 추가
  - 제외 경로 예: `/museum/api/qrcode.do`, `/ux/content/images/common/*`, `/design/common/images/*`, `/design/content/museum/images/*`
  - 검증 결과: `crosseditor/binary/images/*` 실제 전시 이미지가 대표/부가 이미지로 저장됨

## 4) 최종 결론

- [x] 전수 대상 12개 사이트(옵션 1개 포함) 모두 최신 수집 상태 `accepted` 확인
- [x] 배포 전 수집 경로 확정 기준 충족
